# features/

기능 단위 모듈. 각 feature는 `components/`, `hooks/`, `services/`, `stores/`(선택)로 구성합니다.

| Feature | 역할 |
|---------|------|
| `workspace` | 메인 화면 조합 |
| `upload` | 업로드 파이프라인 |
| `preview` | Remotion 미리보기 |
| `export` | outputs/ export |
| `presets` | SNS 프리셋 |
| `queue` | 진행률 UI |
